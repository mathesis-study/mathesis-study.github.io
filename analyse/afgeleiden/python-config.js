window.MathesisPython = {"runtime": "python-runtime-8235805d9ff4.js", "worker": "/* Python loopt buiten de pagina. Stop be\u00ebindigt deze worker volledig.\n * De lokale bundel levert alle bestanden, ook bij openen via file://. */\n\"use strict\";\n\nlet python;\nconst hulpprogramma = `\nimport ast, builtins, contextlib, io, json, sys, traceback\n\nnaamruimten = {}\n\nclass BegrensdeUitvoer(io.StringIO):\n    def __init__(self, rechtstreeks=False):\n        super().__init__()\n        self.rechtstreeks = rechtstreeks\n\n    def write(self, tekst):\n        if self.tell() + len(tekst) > 12000:\n            raise RuntimeError(\"Te veel uitvoer. Controleer of je lus stopt.\")\n        aantal = super().write(tekst)\n        if self.rechtstreeks:\n            _stuur_uitvoer(tekst)\n        return aantal\n\nclass InvoerNodig(Exception):\n    def __init__(self, vraag, nummer, positie=None):\n        self.vraag = vraag\n        self.nummer = nummer\n        self.positie = positie\n\ndef maak_invoer(invoer, posities=None):\n    regels = iter(invoer.splitlines() if isinstance(invoer, str) else invoer)\n    nummer = 0\n    per_regel = {}\n    def lees(vraag=\"\"):\n        nonlocal nummer\n        regel = sys._getframe(1).f_lineno\n        volgnummer = per_regel.get(regel, 0)\n        per_regel[regel] = volgnummer + 1\n        print(vraag, end=\"\")\n        try:\n            antwoord = next(regels)\n        except StopIteration:\n            positie = (posities or {}).get(regel, [])[volgnummer:volgnummer + 1]\n            raise InvoerNodig(vraag, nummer, positie[0] if positie else None) from None\n        nummer += 1\n        print(antwoord)\n        return antwoord\n    return lees\n\ndef maak_omgeving(invoer, posities=None):\n    return {\"__name__\": \"__main__\", \"__builtins__\": dict(vars(builtins), input=maak_invoer(invoer, posities))}\n\ndef omgeving_voor(naam, invoer, posities=None):\n    if not naam:\n        return maak_omgeving(invoer, posities)\n    omgeving = naamruimten.get(naam)\n    if omgeving is None:\n        omgeving = maak_omgeving(invoer, posities)\n        naamruimten[naam] = omgeving\n    else:\n        omgeving[\"__builtins__\"][\"input\"] = maak_invoer(invoer, posities)\n    return omgeving\n\ndef invoerposities(boom):\n    \"\"\"Geef per regel de bronbereiken van aanroepen van de ingebouwde input.\"\"\"\n    posities = {}\n    aanroepen = [knoop for knoop in ast.walk(boom)\n                 if isinstance(knoop, ast.Call) and isinstance(knoop.func, ast.Name)\n                 and knoop.func.id == \"input\"]\n    for knoop in sorted(aanroepen, key=lambda knoop: (knoop.lineno, knoop.col_offset)):\n        positie = {\"regel\": knoop.lineno, \"kolom\": knoop.col_offset,\n                   \"eindregel\": knoop.end_lineno, \"eindkolom\": knoop.end_col_offset}\n        posities.setdefault(knoop.lineno, []).append(positie)\n    return posities\n\ndef foutmelding(exc, schoon=False):\n    spoor = exc.__traceback__\n    if schoon:\n        # Toon enkel de regels van de leerling, niet die van de uitvoerder.\n        while spoor is not None and spoor.tb_frame.f_code.co_filename != \"<oefening>\":\n            spoor = spoor.tb_next\n    fout = \"\".join(traceback.format_exception(type(exc), exc, spoor))[-4000:]\n    regel = getattr(exc, \"lineno\", None)\n    kolom = getattr(exc, \"offset\", None)\n    eindregel = getattr(exc, \"end_lineno\", None)\n    eindkolom = getattr(exc, \"end_offset\", None)\n    if not regel:\n        frames = traceback.extract_tb(exc.__traceback__)\n        oefening = [frame for frame in frames if frame.filename == \"<oefening>\"]\n        if oefening:\n            regel = oefening[-1].lineno\n    gegevens = None\n    if regel:\n        gegevens = {\"soort\": type(exc).__name__, \"bericht\": str(exc),\n                    \"regel\": regel, \"kolom\": kolom,\n                    \"eindregel\": eindregel, \"eindkolom\": eindkolom}\n    return fout, gegevens\n\ndef momentopname(frame):\n    \"\"\"De waarden die de leerling herkent: gewone getallen, tekst en kleine\n    collecties, hoogstens twaalf, met een naam die niet met _ begint.\"\"\"\n    waarden = {}\n    for naam, waarde in frame.f_locals.items():\n        if naam.startswith(\"_\"):\n            continue\n        if type(waarde) in (int, float, str, bool, type(None), list, tuple, dict, set):\n            waarden[naam] = repr(waarde)[:100]\n            if len(waarden) == 12:\n                break\n    return waarden\n\ndef lusbanden(boom):\n    \"\"\"Beschrijf elke for-lus over een range met vaste grenzen: haar waarden en\n    haar stopwaarde. Daarmee toont de pagina naast de code het bandje waarop je\n    ziet dat de lus vlak voor stop ophoudt. Een range met een variabele grens of\n    met meer dan veertien waarden krijgt geen bandje.\"\"\"\n    banden = []\n    for knoop in ast.walk(boom):\n        if not isinstance(knoop, ast.For) or not isinstance(knoop.target, ast.Name):\n            continue\n        aanroep = knoop.iter\n        if not (isinstance(aanroep, ast.Call) and isinstance(aanroep.func, ast.Name)\n                and aanroep.func.id == \"range\" and not aanroep.keywords\n                and 1 <= len(aanroep.args) <= 3):\n            continue\n        try:\n            argumenten = [ast.literal_eval(arg) for arg in aanroep.args]\n        except (ValueError, TypeError, SyntaxError, MemoryError, RecursionError):\n            continue\n        if not all(type(getal) is int for getal in argumenten) or argumenten[-1] == 0:\n            continue\n        waarden = list(range(*argumenten))\n        if len(waarden) > 14:\n            continue\n        banden.append({\"regel\": knoop.lineno, \"eindregel\": knoop.end_lineno,\n                       \"variabele\": knoop.target.id, \"waarden\": waarden,\n                       \"stop\": argumenten[1] if len(argumenten) > 1 else argumenten[0]})\n    return banden\n\ndef condities(boom):\n    \"\"\"Geef per regel met een if, elif of while de test, klaar om te evalueren.\n    Een test met een aanroep blijft erbuiten: die opnieuw uitvoeren zou iets\n    kunnen doen.\"\"\"\n    testen = {}\n    for knoop in ast.walk(boom):\n        if not isinstance(knoop, (ast.If, ast.While)):\n            continue\n        if any(isinstance(deel, (ast.Call, ast.Await, ast.NamedExpr, ast.Yield))\n               for deel in ast.walk(knoop.test)):\n            continue\n        testen[knoop.lineno] = (compile(ast.Expression(knoop.test), \"<oefening>\", \"eval\"),\n                                ast.unparse(knoop.test))\n    return testen\n\ndef regelstappen(bron, invoer=\"\", namespace=\"\"):\n    \"\"\"Voer de code uit en onthoud per uitgevoerde regel de waarden van de\n    variabelen en wat die regel afdrukte. Anders dan instructies() gaat deze\n    opname de lus in: elke ronde levert haar eigen stappen op. Enkel de regels\n    van het hoofdprogramma komen erin, niet die binnen een functie.\"\"\"\n    uitvoer = BegrensdeUitvoer()\n    lijst = []\n    fout = foutgegevens = None\n    lopend = {\"stap\": None, \"positie\": 0}\n    afgekapt = False\n    testen = {}\n\n    def sluit_af():\n        if lopend[\"stap\"] is not None:\n            lopend[\"stap\"][\"uitvoer\"] = uitvoer.getvalue()[lopend[\"positie\"]:]\n            lopend[\"stap\"] = None\n\n    def volg(frame, gebeurtenis, arg):\n        nonlocal afgekapt\n        if frame.f_code.co_filename != \"<oefening>\" or frame.f_code.co_name != \"<module>\":\n            return None\n        if gebeurtenis not in (\"line\", \"return\"):\n            return volg\n        sluit_af()\n        if gebeurtenis == \"return\":\n            return volg\n        if len(lijst) >= 400:\n            afgekapt = True\n            return volg\n        stap = {\"regel\": frame.f_lineno, \"eindregel\": frame.f_lineno,\n                \"waarden\": momentopname(frame), \"uitvoer\": \"\"}\n        test = testen.get(frame.f_lineno)\n        if test is not None:\n            try:\n                stap[\"conditie\"] = {\"tekst\": test[1],\n                                    \"waarde\": bool(eval(test[0], frame.f_globals, frame.f_locals))}\n            except BaseException:\n                pass  # Een test die nu niet kan, blijft gewoon weg.\n        lijst.append(stap)\n        lopend[\"stap\"] = stap\n        lopend[\"positie\"] = uitvoer.tell()\n        return volg\n\n    banden = []\n    try:\n        boom = ast.parse(bron, filename=\"<oefening>\")\n        omgeving = omgeving_voor(namespace, invoer, invoerposities(boom))\n        banden = lusbanden(boom)\n        testen = condities(boom)\n        with contextlib.redirect_stdout(uitvoer), contextlib.redirect_stderr(uitvoer):\n            try:\n                sys.settrace(volg)\n                exec(compile(boom, \"<oefening>\", \"exec\"), omgeving)\n            finally:\n                sys.settrace(None)\n                sluit_af()\n    except InvoerNodig as exc:\n        return {\"instructies\": lijst, \"lussen\": banden, \"afgekapt\": afgekapt,\n                \"fout\": None, \"foutgegevens\": None, \"uitvoer\": uitvoer.getvalue(),\n                \"invoer\": {\"vraag\": exc.vraag, \"nummer\": exc.nummer, \"positie\": exc.positie}}\n    except BaseException as exc:\n        fout, foutgegevens = foutmelding(exc, schoon=True)\n    return {\"instructies\": lijst, \"lussen\": banden, \"afgekapt\": afgekapt,\n            \"fout\": fout, \"foutgegevens\": foutgegevens, \"invoer\": None}\n\ndef instructies(bron, invoer=\"\", namespace=\"\"):\n    \"\"\"Voer de code instructie per instructie uit en onthoud per instructie\n    haar regels en wat ze afdrukte, zodat de pagina ze een voor een kan tonen.\"\"\"\n    uitvoer = BegrensdeUitvoer()\n    lijst = []\n    fout = foutgegevens = None\n    try:\n        boom = ast.parse(bron, filename=\"<oefening>\")\n        omgeving = omgeving_voor(namespace, invoer, invoerposities(boom))\n        with contextlib.redirect_stdout(uitvoer), contextlib.redirect_stderr(uitvoer):\n            for nummer, knoop in enumerate(boom.body):\n                begin = min([knoop.lineno] + [d.lineno for d in getattr(knoop, \"decorator_list\", [])])\n                stap = {\"regel\": begin, \"eindregel\": knoop.end_lineno, \"uitvoer\": \"\"}\n                lijst.append(stap)\n                voor = uitvoer.tell()\n                try:\n                    if nummer == len(boom.body) - 1 and isinstance(knoop, ast.Expr):\n                        waarde = eval(compile(ast.Expression(knoop.value), \"<oefening>\", \"eval\"), omgeving)\n                        if waarde is not None:\n                            print(repr(waarde))\n                    else:\n                        exec(compile(ast.Module([knoop], []), \"<oefening>\", \"exec\"), omgeving)\n                finally:\n                    stap[\"uitvoer\"] = uitvoer.getvalue()[voor:]\n    except InvoerNodig as exc:\n        return {\"instructies\": lijst, \"fout\": None, \"foutgegevens\": None,\n                \"uitvoer\": uitvoer.getvalue(),\n                \"invoer\": {\"vraag\": exc.vraag, \"nummer\": exc.nummer, \"positie\": exc.positie}}\n    except BaseException as exc:\n        fout, foutgegevens = foutmelding(exc, schoon=True)\n    return {\"instructies\": lijst, \"fout\": fout, \"foutgegevens\": foutgegevens,\n            \"invoer\": None}\n\ndef uitvoeren(bron, invoer=\"\", volgen=False, namespace=\"\", rechtstreeks=False):\n    uitvoer = BegrensdeUitvoer(rechtstreeks)\n    stappen = []\n    laatst = None\n    omgeving = None\n    def snapshot(frame, regel):\n        if len(stappen) >= 150:\n            return\n        stappen.append({\"regel\": regel, \"waarden\": momentopname(frame)})\n    def volg(frame, gebeurtenis, arg):\n        nonlocal laatst\n        if frame.f_code.co_filename == \"<oefening>\" and frame.f_code.co_name == \"<module>\":\n            if gebeurtenis in (\"line\", \"return\"):\n                if laatst is not None:\n                    snapshot(frame, laatst)\n                laatst = frame.f_lineno if gebeurtenis == \"line\" else None\n        return volg\n    fout = None\n    foutgegevens = None\n    waarde = None\n    invoervraag = None\n    try:\n        boom = ast.parse(bron, filename=\"<oefening>\")\n        omgeving = omgeving_voor(namespace, invoer, invoerposities(boom))\n        laatste = boom.body.pop() if boom.body and isinstance(boom.body[-1], ast.Expr) else None\n        with contextlib.redirect_stdout(uitvoer), contextlib.redirect_stderr(uitvoer):\n            try:\n                if volgen:\n                    sys.settrace(volg)\n                exec(compile(boom, \"<oefening>\", \"exec\"), omgeving)\n                if laatste:\n                    waarde = eval(compile(ast.Expression(laatste.value), \"<oefening>\", \"eval\"), omgeving)\n                    if waarde is not None:\n                        print(repr(waarde))\n            finally:\n                sys.settrace(None)\n    except InvoerNodig as exc:\n        invoervraag = {\"vraag\": exc.vraag, \"nummer\": exc.nummer, \"positie\": exc.positie}\n    except BaseException as exc:\n        fout, foutgegevens = foutmelding(exc)\n    return {\"uitvoer\": uitvoer.getvalue(), \"fout\": fout,\n            \"foutgegevens\": foutgegevens, \"stappen\": stappen, \"waarde\": waarde,\n            \"invoer\": invoervraag, \"omgeving\": omgeving}\n\ndef opdracht(bericht):\n    stapsgewijs = bericht.get(\"stapsgewijs\")\n    if stapsgewijs:\n        stappen = regelstappen if stapsgewijs == \"regel\" else instructies\n        return json.dumps(stappen(bericht[\"bron\"], bericht.get(\"invoer\", \"\"), bericht.get(\"namespace\", \"\")), ensure_ascii=False)\n    rechtstreeks = bool(bericht.get(\"outputDelay\"))\n    resultaat = uitvoeren(bericht[\"bron\"], bericht.get(\"invoer\", \"\"), bericht[\"naam\"] == \"wisselen\", bericht.get(\"namespace\", \"\"), rechtstreeks)\n    feedback = None if resultaat[\"invoer\"] else controleer(bericht[\"naam\"], bericht[\"bron\"], uitvoeren)\n    goed = feedback.startswith(\"Goed:\") if feedback is not None else None\n    onbeoordeeld = False\n    if feedback is None and not resultaat[\"fout\"] and not resultaat[\"invoer\"]:\n        onbeoordeeld = True\n        if bericht.get(\"modeloplossing\"):\n            feedback = \"Dit is een voorbeeld van een juiste oplossing.\"\n            goed = True\n        else:\n            feedback = (\"Je programma voert foutloos uit. Controleer nu zelf of het ook \"\n                        \"precies doet wat de opgave vraagt.\")\n    antwoord = {k: resultaat[k] for k in (\"uitvoer\", \"fout\", \"foutgegevens\", \"stappen\")}\n    antwoord[\"invoer\"] = resultaat[\"invoer\"]\n    antwoord.update({\"feedback\": feedback, \"goed\": goed, \"onbeoordeeld\": onbeoordeeld})\n    return json.dumps(antwoord, ensure_ascii=False)\n`;\n\nfunction bytes(base64) {\n  return Uint8Array.from(atob(base64), teken => teken.charCodeAt(0));\n}\n\nself.onmessage = async function (event) {\n  const bericht = event.data;\n  try {\n    if (bericht.actie === \"init\") {\n      const bestanden = bericht.bestanden;\n      const inhoud = {\n        \"pyodide.asm.wasm\": bytes(bestanden.wasm),\n        \"python_stdlib.zip\": bytes(bestanden.stdlib),\n        \"pyodide-lock.json\": bestanden.lock\n      };\n      // Geen netwerkfallback: ontbrekende packages blijven expliciet ontbreken.\n      self.fetch = async function (url) {\n        const naam = String(url.url || url).split(\"/\").pop();\n        if (!(naam in inhoud)) throw new Error(\"Niet meegeleverd Pythonbestand: \" + naam);\n        return new Response(inhoud[naam], { headers: {\n          \"Content-Type\": naam.endsWith(\"wasm\") ? \"application/wasm\" : \"application/octet-stream\"\n        } });\n      };\n      const urls = [bestanden.loader, bestanden.asm].map(bron =>\n        URL.createObjectURL(new Blob([bron], { type: \"text/javascript\" })));\n      try {\n        importScripts(...urls);\n        python = await loadPyodide({ indexURL: \"https://python.invalid/\", fullStdLib: false });\n      } finally {\n        urls.forEach(url => URL.revokeObjectURL(url));\n      }\n      python.runPython(hulpprogramma + \"\\n\" + bericht.controle);\n      self.postMessage({ klaar: true });\n      return;\n    }\n    python.globals.set(\"_bericht_json\", JSON.stringify(bericht));\n    python.globals.set(\"_stuur_uitvoer\", tekst => self.postMessage({ uitvoerDeel: String(tekst) }));\n    const resultaat = python.runPython(\"opdracht(json.loads(_bericht_json))\");\n    self.postMessage(JSON.parse(resultaat));\n  } catch (fout) {\n    self.postMessage({ fout: String(fout.message || fout) });\n  }\n};\n", "controle": "def controleer(*args): return None\n"};
